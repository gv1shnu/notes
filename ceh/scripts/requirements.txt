# Pentest notes automation — install: pip install -r requirements.txt
# Implementations live as you fill in TODOs on each HTML page (see <!-- comments -->).

requests>=2.31.0
beautifulsoup4>=4.12.0
dnspython>=2.6.0
python-nmap>=0.7.1
boto3>=1.34.0
jinja2>=3.1.0
pyyaml>=6.0.0

# Optional — uncomment when needed:
# scapy>=2.5.0
# pyshark>=0.6.0
# playwright>=1.40.0
